<html>
  <head>
    <title><%= headTitle %></title>
    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body>
    <main>
      <%- include('partials/navigation', { menuLinks: menuLinks, activeLinkPath:
      activeLinkPath }); %>
      <h1 class="heading">Products</h1>
      <% if (products?.length > 0) { %>
      <ul>
        <% products.forEach(product => { %>
        <li>
          Name: <%= product.name %>, Description: <%= product.description %><a href="/products/<%= product.name %>">Details</a><button onclick="deleteProduct('<%= product.name %>')">Delete</button>
        </li>
        <% }); %>
      </ul>
      <% } else { %>
      <br />
      <div>No products available.</div>
      <% }%>
    </main>
  </body>
  <%- include('partials/deleteProductScript'); %>
</html>
